// import { toLocalX } from "../functions/functions.js";
// // import InsertSymbol from "../draws/InsertSymbol.js";
// // import createSymbolSVG from "../draws/_createSymbolSVG.js";
// import insertSymbol from "../functions/insertSymbol.js";

export default function handleClick(e, ref) {
  console.log(e.offsetX);
  // // روش گرفتن المنت از آی دی با وانیلا جی‌اس
  // // container = document.getElementById(container);
  // const svgNS = "http://www.w3.org/2000/svg";
  // // یک بوردر برای اس‌ وی جی به تمام پهنا و ارتفاع رسم می‌کنیم
  // const rect = document.createElementNS(svgNS, "rect");
  // rect.setAttribute("x", 0);
  // rect.setAttribute("y", 0);
  // rect.setAttribute("width", 50);
  // rect.setAttribute("height", 50);
  // rect.setAttribute("fill", "transparent");
  // rect.setAttribute("stroke", "red");
  // // container.appendChild(rect);
  // // console.log(container);

  // // روش رف ری‌اکت

  // console.log(ref.current);
  // insertSymbol(ref.current, "R_AC", 0, 0, 10, 10, "R_AC");
  // // ref.current.appendChild(createSymbolSVG("R_AC"));
}
